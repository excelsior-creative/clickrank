# Journal — 2026-04-17 (first CEO session)

## Arrival state

- No `/ceo` folder existed. First session.
- Branch: `claude/great-sagan-8QnZ7` (clean working tree at start).
- Repo: Next.js 16 + Payload CMS 3 monorepo. Single app at `apps/app`.
- Nightly pipeline exists at `/api/cron/generate-article`. Uses
  Gemini 2.5 Flash preview for article gen, Replicate
  (nano-banana-pro) for featured image, Payload for storage.
- Vercel cron scheduled at 10:00 UTC daily (see `vercel.json`).

## Inspection findings

- **Compliance gap (critical):** No FTC affiliate disclosure
  rendered anywhere on the site. Posts have been publishing
  (per Brandon's mandate) without disclosure. Legal exposure.
- **SEO gap:** `generateArticleSchema` helper existed in
  `lib/structured-data.ts` but was never injected into the blog
  post page. Published posts had no Article schema.
- **Pipeline gap:** Content generation prompt did not require
  disclosure in the body either — so even the markdown fallback
  was missing.
- **Other gaps logged in `backlog.md`:** dead social links,
  decorative newsletter input, `/blog` uses `force-dynamic`
  killing ISR, fuzzy dedup logic in pipeline.
- Couldn't reach live site from sandbox (403 outside allowlist).
  Also can't query production Postgres or Vercel logs — logged
  in `INBOX.md`.

## What shipped tonight

- **`/ceo` folder scaffolded** with all required files:
  README, strategy, okrs, roadmap, backlog, pipeline, editorial,
  products, seo, monetization, metrics, competitive, experiments,
  INBOX.
- **`AffiliateDisclosure` component** added
  (`src/components/AffiliateDisclosure.tsx`) with three
  variants: banner, inline, footer.
- **Blog post page** now renders:
  - banner disclosure above the title
  - inline disclosure + correction-email invitation below the body
  - `Article` + `BreadcrumbList` JSON-LD combined graph schema
- **Footer** now renders a one-line site-wide disclosure.
- **Content generation prompt** rewritten to:
  - bake an "Affiliate disclosure" H2 section at the top of every
    generated review (belt-and-suspenders with template render)
  - require an honest "What could be better" caveats section
  - forbid fabricated first-person, testimonials, studies
  - forbid medical/financial/income claims
  - match the Honest-Favorable editorial standard
- **Decision record DR-0001** authored describing the disclosure
  rollout.

## What I did NOT do tonight

- **No pipeline run.** First session — priority was making sure
  the pipeline's output renders legally before we add more posts
  to the index. The cron will run at its scheduled time anyway;
  any draft it produces tomorrow will benefit from tonight's
  fixes.
- **No `affiliateUrl` field on Posts.** That's a schema migration
  and goes through PR per the mandate. Queued in `roadmap.md` as
  the top "Next" item.
- **No outbound click tracking.** Same — needs a deliberate design
  decision, logged in backlog.
- **No QA stage yet.** Documented as top pipeline priority.

## Changes & risk

- All changes are additive / render-layer. No database migration.
  No collection-schema changes. No cron-schedule changes.
- `AffiliateDisclosure` component is brand-new; used in two
  places (post page, footer).
- Blog post page now injects JSON-LD. Output is schema-validator
  safe (I hand-inspected the combined graph shape).
- Pipeline prompt change affects only NEW generated articles.
  Existing drafts/published posts are untouched.

## Verification I couldn't do

- Sandbox has no network access to clickrank.net or to install
  `pnpm` deps, so I couldn't run `pnpm type-check` or visually
  verify the pages render. Opening as a **draft PR** so CI or
  Brandon can verify before merge.

## Pipeline run summary

- Generated: 0
- Published: 0
- Rejected at QA: 0 (no QA stage)
- Errors: none (no run attempted)

## Metrics delta

- No new numbers (no data access this run).
- First baseline set in `metrics.md`; most rows "unknown".

## Asks for Brandon

See `INBOX.md`. Summary:
- 🟡 Data access (GSC, ClickBank commission feed, Vercel,
  read-only DB).
- 🟡 Confirm the nightly cron is firing in production.
- 🟢 Legal scan recommended for posts published before tonight —
  they were missing disclosure.

## What's queued for next night

1. Open the PR, get it reviewed/merged.
2. Audit posts already in the DB (need DB access or a manual
   list from Brandon).
3. Open a PR adding the `affiliateUrl` field to the Posts
   collection + wiring the pipeline to save it + rendering a
   CTA button on the post page.
4. Start designing the QA stage for the pipeline.

## Self-assessment

Solid foundation night. Didn't publish new content, but closed
a legal/editorial gap that would have compounded as a liability
every night we didn't notice it. The pipeline's output is now
safe to render. Next session should produce real content.
